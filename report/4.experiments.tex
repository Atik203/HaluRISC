% Chapter 4 — Experiments (setup, model comparison, calibration,
% ablation, statistics, efficiency, judge, zero-shot transfer).
\section{Experiments}

\subsection{Setup}

All results below are computed on the locked test split (3{,}000 samples) with the calibrator fit on validation only. Metrics: precision, recall, F1, AUROC, PR-AUC, MCC, ECE, Brier. Significance: McNemar's test on paired test predictions, bootstrap 95\% confidence intervals (1{,}000 resamples), Wilcoxon signed-rank across seeds.

\subsection{Model comparison}

Table~\ref{tab:models} reports test-set performance. All trained models perform strongly, confirming that HaluEval QA is a comparatively easy benchmark for supervised classifiers; the engineered features are highly informative. XGBoost and random forest produce near-identical predictions (McNemar $p=1.0$; zero discordant pairs), and the differences between XGBoost and logistic regression are not statistically significant on this benchmark (McNemar $p=0.44$). The honest conclusion is that all supervised models comfortably outperform the overlap heuristic and that benchmark saturation, rather than model choice, dominates this comparison.

\begin{table}[t]
\centering
\small
\caption{Test-set performance (mean over seeds 42/123/456). XGBoost and RF produce identical predictions on this test set (std $=0$ across seeds).}
\label{tab:models}
\begin{tabular}{@{}lcccccc@{}}
\toprule
\textbf{Model} & \textbf{P} & \textbf{R} & \textbf{F1} & \textbf{AUROC} & \textbf{PR-AUC} & \textbf{MCC} \\
\midrule
Heuristic (1$-$overlap) & 0.9392 & 0.9467 & 0.9429 & 0.9148 & 0.8117 & 0.8854 \\
Logistic Regression     & 0.9804 & 0.9693 & 0.9749 & 0.9943 & 0.9948 & 0.9501 \\
Random Forest           & 0.9915 & 0.9858 & 0.9886 & 0.9982 & 0.9987 & 0.9774 \\
\textbf{XGBoost (ours)} & \textbf{0.9919} & \textbf{0.9853} & \textbf{0.9886} & \textbf{0.9980} & \textbf{0.9987} & \textbf{0.9774} \\
\bottomrule
\end{tabular}
\end{table}

Bootstrap 95\% CIs for XGBoost: F1 $[0.9845,\,0.9924]$, AUROC $[0.9962,\,0.9994]$.

\subsection{Calibration}

Table~\ref{tab:calib} compares raw, Platt-calibrated, and isotonic-calibrated probabilities. Two findings deserve emphasis. First, XGBoost trained with a logistic objective is already well calibrated (raw ECE 0.0115), so Platt scaling yields no further gain---an honest negative result. Second, isotonic regression improves ECE 2.3$\times$ (to 0.0051) with no loss in F1. The deployed artifact ships the Platt-calibrated model (blueprint default); isotonic is available and would be preferred when calibration is the primary criterion.

\begin{table}[t]
\centering
\small
\caption{Calibration comparison on the test set (mean over seeds).}
\label{tab:calib}
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Method} & \textbf{F1} & \textbf{ECE} & \textbf{Brier} \\
\midrule
Raw XGBoost        & 0.9886 & 0.0115 & 0.0085 \\
Platt (sigmoid)    & 0.9887 & 0.0116 & 0.0092 \\
Isotonic           & 0.9885 & \textbf{0.0051} & 0.0089 \\
\bottomrule
\end{tabular}
\end{table}

The reliability diagram (Figure~\ref{fig:reliability}) shows the calibrated curve closely tracking the diagonal.

\begin{figure}[t]
\centering
\includegraphics[width=0.62\textwidth]{fig_reliability.pdf}
\caption{Reliability diagram of the calibrated model (ECE 0.0116 Platt / 0.0051 isotonic).}
\label{fig:reliability}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\textwidth]{fig_roc_pr.pdf}
\caption{ROC and precision-recall curves on the test set (AUROC 0.9980, PR-AUC 0.9987).}
\label{fig:rocpr}
\end{figure}

\subsection{Ablation study}

Table~\ref{tab:ablation} removes each feature group independently (3 seeds per removal). Lexical overlap is the most important group (F1 drops by 0.0183 when removed); length features also matter. Removing the entity group slightly \emph{improves} F1---a negative result we report rather than hide: on this benchmark, spaCy entity features add no value. NLI features contribute a modest but consistent improvement (F1 $-$0.0028 when removed), below the lexical signal but above entity features.

\begin{table}[t]
\centering
\small
\caption{Feature-group ablation (F1/AUROC, mean $\pm$ std over 3 seeds; full model F1 0.9886, AUROC 0.9980).}
\label{tab:ablation}
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Removed group} & \textbf{F1} & \textbf{$\Delta$F1} & \textbf{AUROC} \\
\midrule
length   & 0.9851 $\pm$ 0.0002 & $-$0.0036 & 0.9970 \\
lexical  & 0.9703 $\pm$ 0.0003 & $-$0.0183 & 0.9943 \\
entity   & 0.9890 $\pm$ 0.0000 & $+$0.0003 & 0.9980 \\
nli      & 0.9858 $\pm$ 0.0002 & $-$0.0028 & 0.9979 \\
numeric  & 0.9887 $\pm$ 0.0002 & $+$0.0001 & 0.9981 \\
hedging  & 0.9887 $\pm$ 0.0002 & $+$0.0001 & 0.9981 \\
semantic & 0.9889 $\pm$ 0.0003 & $+$0.0002 & 0.9980 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Statistical testing}

McNemar's test on the 3{,}000 test predictions: XGBoost vs.\ random forest $p=1.0$ (identical predictions), vs.\ logistic regression $p=0.44$, vs.\ heuristic $p=0.09$. These results are honest and informative: the supervised models commit largely overlapping errors on this benchmark, and their differences are not statistically significant; the headline claims of this paper therefore rest on calibration quality, efficiency, interpretability, and the generalization analysis---not on a marginal accuracy advantage.

\subsection{Efficiency and cost}

Table~\ref{tab:latency} reports per-component latency on 200 test samples (p50/p95). Feature extraction dominates (NER 44.7\,ms, NLI 61.4\,ms); model inference is 5.2\,ms and SHAP 9.5\,ms. The total is $\approx$137\,ms per sample on the reference laptop (RTX 3060 6\,GB, CPU-bound spaCy/NLI on GPU).

\begin{table}[t]
\centering
\small
\caption{Per-component latency (ms), n=200.}
\label{tab:latency}
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Component} & \textbf{p50} & \textbf{p95} & \textbf{mean} \\
\midrule
Core lexical (4 groups) & 0.14 & 0.22 & 0.15 \\
Entity (spaCy NER)      & 40.9 & 76.0 & 44.7 \\
NLI (2 directions)      & 53.9 & 67.0 & 61.4 \\
Semantic (MiniLM)       & 14.8 & 21.7 & 16.0 \\
XGBoost inference       & 4.5 & 6.1 & 5.2 \\
SHAP explanation        & 7.4 & 31.4 & 9.5 \\
\midrule
\textbf{Total per sample} & \textbf{$\approx$137} & & \\
\bottomrule
\end{tabular}
\end{table}

\subsection{LLM-as-judge comparison}

Table~\ref{tab:judge} compares HaluRISC against GPT 5.6 Luna as an LLM judge on the same 200 balanced test samples (measured, not estimated). The judge achieves F1 0.82 with 1.2\,s median latency at \$0.101 per 1{,}000 predictions; HaluRISC achieves F1 0.99 at $\approx$5\,ms and near-zero marginal cost. McNemar on the paired verdicts is highly significant ($p = 3\times10^{-7}$): the difference is not sampling noise. We note that judge scores vary run-to-run because the API rejects temperature 0 for this model.

\begin{table}[t]
\centering
\small
\caption{LLM-as-judge vs.\ XGBoost on 200 test samples.}
\label{tab:judge}
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Model} & \textbf{Acc} & \textbf{P} & \textbf{R} & \textbf{F1} & \textbf{Cost/1K} \\
\midrule
GPT 5.6 Luna judge & 0.840 & 0.947 & 0.720 & 0.818 & \$0.101 \\
\textbf{XGBoost}    & \textbf{0.990} & \textbf{1.000} & \textbf{0.980} & \textbf{0.990} & $\approx$\$0.001 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{External zero-shot evaluation (RAGTruth)}

Table~\ref{tab:ragtruth} reports zero-shot evaluation on 2{,}000 natural RAGTruth \cite{ragtruth} QA responses (ACL 2024, human word-level annotations) with no training or adaptation. The model fails to transfer: recall 1.0 means it flags nearly everything as hallucinated. This is a genuine and important negative result---synthetic HaluEval hallucinations differ systematically from naturally generated RAG responses---and it frames generalization as open future work rather than claiming universal detection.

\begin{table}[t]
\centering
\small
\caption{RAGTruth zero-shot (n=2{,}000, labels: 631 hallucinated / 1{,}369 grounded).}
\label{tab:ragtruth}
\begin{tabular}{@{}lcccccc@{}}
\toprule
\textbf{F1} & \textbf{AUROC} & \textbf{PR-AUC} & \textbf{MCC} & \textbf{ECE} & \textbf{Brier} \\
\midrule
0.4822 & 0.5869 & 0.3601 & 0.0570 & 0.6661 & 0.6611 \\
\bottomrule
\end{tabular}
\end{table}
